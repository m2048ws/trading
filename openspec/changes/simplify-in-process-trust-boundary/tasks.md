## 1. Trust Contract, Inventory, and Regression Guard

- [ ] 1.1 Confirm the effective RFC/source/traceability binding, isolated delivery worktree and branch, exact
  `origin/main` baseline, pilot authority, closed native dependencies, and clean pre-implementation build state.
- [ ] 1.2 Inventory every production/benchmark method handle, reflective private-member call, erased recovery cast,
  `private[this]` construction site, runtime exact-class guard, and related adversarial fixture; classify each invariant
  as field-valid, checked construction, derived evidence/state, or checked observation in the Group implementation note.
- [ ] 1.3 Reconcile `docs/design-principles.md`, architecture/audit/module guidance, and representative examples so
  cooperative in-process code, untrusted external data, retained semantic capabilities, unsupported JVM internals, and
  future real isolation requirements are explicit and consistent with the delta specs.
- [ ] 1.4 Add a deterministic repository regression check proving production and benchmark source contains no
  `MethodHandle`, `MethodHandles`, `privateLookupIn`, reflective constructor, or reflective private-member access,
  without prohibiting useful test-only artifact inspection.
- [ ] 1.5 Run formatting, documentation/spec validation, the new source guard, dependency checks, and focused
  architecture/functional-design tests; record the accepted inventory and baseline without changing runtime behavior.

## 2. Order Model Static Construction and Semantic Evidence

- [ ] 2.1 Replace dynamic construction of checked activation, fixed/trailing trigger evidence, peg resolution, order
  intent, and aggregate order with direct companion/member construction at the narrowest readable visibility; remove
  method descriptors, invocation recovery casts, and private-access annotations made obsolete by the rewrite.
- [ ] 2.2 Preserve trigger satisfaction and instruction/evidence agreement, peg offset and instruction agreement,
  positive lots/prices/offsets, position-change derivation, deterministic aggregate violations, and all associated
  evidence result types at their checked owning operations.
- [ ] 2.3 Remove hostile foreign-subclass/exact-runtime-class guards from order activation, pricing, execution, and
  visibility while retaining sealed/closed alternatives, exhaustive matching, typed invalid combinations, and ordinary
  erased-input defenses.
- [ ] 2.4 Rewrite order unit and completed-artifact fixtures around domain factory behavior, same-shape semantic
  mismatches, unsupported associated shapes, exhaustive ADTs, Scala inference, and ordinary Java use without reflection
  or constructor-modifier assertions.
- [ ] 2.5 Format and run order-model compilation/tests plus dependent scenario, fee, risk, codec, lifecycle, and packaged
  compiler boundaries needed to prove the static API preserves observable behavior.

## 3. Execution Scenario Static Construction

- [ ] 3.1 Replace dynamic construction of liquidity slices, non-empty matched slices, assumptions, complete order
  scenarios, and round trips with owner-local direct construction and narrow statically callable operations.
- [ ] 3.2 Preserve positive lots, instrument/grid identity, associated activation and pricing evidence, slice ordering and
  non-emptiness, market-state consistency, entry/exit identity, exact signed flatness, and retained held-position
  derivation before stronger scenario results return.
- [ ] 3.3 Remove invocation-only casts and descriptors while retaining only casts immediately justified by checked
  identity/path-dependent equality; expose no new generic unchecked scenario constructor.
- [ ] 3.4 Add or adapt scenario examples, properties, negative compiler cases, serialization checks, equality/hash tests,
  and ordinary Scala/Java client fixtures so direct construction never substitutes for semantic verification.
- [ ] 3.5 Format and run execution-scenario tests with order/instrument prerequisites and fee, risk, codec, and packaged
  downstream checks.

## 4. Fee Policy and Attribution Static Construction

- [ ] 4.1 Replace dynamic construction in assessed fee values, scenario-fee aggregates, attributed contributions, and
  fee-inclusive PnL with direct owner-defined construction; remove method-handle descriptors and erased invocation
  casts.
- [ ] 4.2 Preserve denomination/instrument identity, grid amount and residual, source leg and market-state attribution,
  non-empty ordered contributions, exact settlement composition, and deterministic typed mismatch failures.
- [ ] 4.3 Keep policy selection and directive validation independent of core fee/PnL ownership, introduce no unchecked
  public reconstruction helper, and retain closed alternatives and serialization rejection.
- [ ] 4.4 Add or adapt fee-policy unit/property/client tests covering charges, rebates, multi-asset settlement, entry/exit
  attribution, invalid directives, identity mismatches, contribution ordering, and exact PnL equivalence.
- [ ] 4.5 Format and run fee-policy compilation/tests plus its instrument/order/scenario prerequisites and packaged
  boundary checks.

## 5. Risk Model, Sizing, and Benchmark Static Access

- [ ] 5.1 Replace dynamic construction and observation for lot-risk assessments, non-empty model violations, monotone
  models, loss formulae, and sizing probes with direct owner-defined calls; remove all risk and benchmark method handles
  and invocation recovery casts.
- [ ] 5.2 Preserve checked assessment identity/dimensions, closed-form and complete-table monotonicity, curve
  compatibility, exact quantization, deterministic violations, exhaustive fallback, binary-search witness boundaries,
  and observation-count bounds.
- [ ] 5.3 Remove hostile subclass/exact-class guards from primary and exhaustive sizing result families while retaining
  sealed alternatives, total matching, exact observations, and Java-serialization rejection.
- [ ] 5.4 Adapt risk unit/property/completed-JAR/Java fixtures and JMH setup to ordinary factories and static observation;
  retain negative arbitrary-function conversion, invalid table/curve, compatibility, exactness, and complexity cases.
- [ ] 5.5 Format and run risk compilation/tests, the source guard, benchmark compilation, and representative sizing
  measurements or operation-count comparisons demonstrating no material hot-path regression.

## 6. Execution Identity, Authority, Ordering, and Lineage

- [ ] 6.1 Replace dynamic construction of application/logical/source/account/native identities and qualified event,
  order, fill, stream, position, and target values with direct companion construction while retaining null/empty/value
  validation at supported representation boundaries.
- [ ] 6.2 Replace dynamic construction of ordering origins/continuations, authoritative sequencing, checkpoints,
  completeness, lineage links, and their non-empty diagnostics with narrow static owning operations.
- [ ] 6.3 Preserve qualification scope, source/account/native provenance, sequence positivity and continuation rules,
  completeness coverage, distinct predecessor/successor identity, and confirmed-cancellation lineage predicates.
- [ ] 6.4 Remove hostile exact-class guards and bytecode-constructor expectations from these closed execution values;
  retain exhaustive alternatives, equality/hash, Java serialization rejection, and ordinary Scala/Java checked clients.
- [ ] 6.5 Format and run focused execution identity/authority/ordering/lineage suites, packaged boundaries, source guard,
  and downstream execution compilation before advancing.

## 7. Execution Commands, Submission Knowledge, and Cancellation

- [ ] 7.1 Replace dynamic construction for command violations, submit/cancel commands, dispatch evidence, command state,
  conflicts, transitions, submission evidence/knowledge, cancellation evidence/knowledge, and cancellation anomalies
  with direct owner-defined operations.
- [ ] 7.2 Preserve command identity/body idempotency, conflicting reuse, target/order/lineage scope, proven-not-dispatched
  versus indeterminate dispatch, authoritative absence, evidence conflicts, and same-command recovery semantics.
- [ ] 7.3 Preserve cancel request versus effective cancellation, source-supported confirmation, partial-fill and
  cancel/fill race behavior, post-cancellation anomaly ordering, and exact exposure without constructor provenance.
- [ ] 7.4 Remove hostile implementation-class guards while retaining closed knowledge alternatives, checked erased
  inputs, deterministic non-empty conflicts, serialization rejection, and ordinary positive/negative client coverage.
- [ ] 7.5 Format and run command, submission, cancellation, lineage, race/permutation, packaged-boundary, and source-guard
  checks against the statically constructed execution model.

## 8. Execution Facts, State, Replay, and Effective Fill Ledger

- [ ] 8.1 Replace dynamic construction for source-fact violations and all fact cases, classifications, source-evidence
  state/results, lifecycle diagnostics/state/results, effective-fill alternatives/ledger, and lifecycle/replay roots
  with direct owner-defined operations.
- [ ] 8.2 Preserve qualified identity and target checks, exact fill economics, duplicate/conflict classification,
  authoritative stream ordering, gaps/rewinds, unresolved correction/bust references, completeness, and deterministic
  transition/replay results.
- [ ] 8.3 Preserve effective fill replacement/bust semantics, ambiguity retention, exact signed exposure, overfill and
  post-cancellation anomalies, delivery permutation equivalence, and indexed complexity without reflective authority.
- [ ] 8.4 Replace hostile constructor/subclass fixtures with least-trusted supported-input tests for every strengthening
  boundary, plus exhaustive ADT, equality/hash, serialization, replay, correction/bust, and Scala/Java client coverage.
- [ ] 8.5 Format and run all execution-lifecycle unit/property/replay/race/complexity and completed-artifact checks, the
  source guard, and dependent aggregate compilation.

## 9. Boundary Codec and Catalog Replay Static Construction

- [ ] 9.1 Replace dynamic construction of envelopes, decode limits, paths, non-empty wire violations, grid-coordinate
  packages, order/scenario records, journal entries, and replay results with direct owner-local construction; remove
  descriptors and invocation recovery casts.
- [ ] 9.2 Preserve strict syntax and duplicate handling, decode limits, exact primitive/refinement decoding, explicit V1
  dispatch, canonical JSON/JCS bytes, coherent snapshot resolution, dependent grid/identity reconstruction, and stable
  deterministic path/error order.
- [ ] 9.3 Preserve order/scenario associated shapes and aggregate checks, journal publication/revision/no-op/conflict
  rules, atomic batch behavior, fresh replay lineage, and distinction between non-authoritative records and trusted
  reconstructed results.
- [ ] 9.4 Adapt codec/journal unit, law, golden-vector, independent-JCS, malformed/limit, replay, serialization, and
  ordinary Scala/Java fixtures to use static factories without weakening any external-data failure path or V1 shape.
- [ ] 9.5 Format and run boundary-codec/catalog-replay tests with all domain prerequisites, source/API scans, schema
  validation, golden vectors, batch/limit matrices, and representative codec/replay performance evidence.

## 10. Adversarial Reconciliation and Whole-Repository Evidence

- [ ] 10.1 Classify every adversarial-boundary and completed-JAR fixture: remove hostile same-package, subclass,
  constructor-modifier, exact-class, or deliberate direct-construction assertions; retain or replace dimension/grid,
  refinement, associated-evidence, dependency, erased-input, external-data, and serialization coverage.
- [ ] 10.2 Rework representative ordinary Scala and Java clients so quantity/reference handles, orders/scenarios, fees,
  risk, execution transitions, and codecs use documented factories with no reflective setup; retain checked failures for
  malformed V1 records and incoherent catalog/execution identities.
- [ ] 10.3 Run formatting and a clean dependency-ordered compile/test matrix for every SBT project, the root aggregate,
  completed artifacts, adversarial boundary, source/dependency/API scans, Java serialization rejection, and JMH
  compilation/representative measurements.
- [ ] 10.4 Inspect final source, packaged APIs, classpaths, closed-match warnings, casts, docs, schemas, golden vectors,
  and test removals to prove no production/benchmark reflection, new unchecked authority, dependency drift, or V1
  compatibility change remains.
- [ ] 10.5 Record AC-001 through AC-006 evidence, complete the Scala/Java and malformed-external-data walkthrough,
  validate OpenSpec and Corgi source/traceability at the final planning revision, and prepare the final acknowledged Task
  Group commit for separate whole-change Verify and human gates.
