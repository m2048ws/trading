## 1. Direct instrument dimension aliases and boundary evidence

- [ ] 1.1 Reconcile the current integrated `Instrument` surface, add `PositionD`, `BaseD`, `QuoteD`, and `SettleD` as exact aliases of the retained role dimensions, and express every applicable existing grid, rate, and nested economic member through them without casts, match types, runtime fields, lookup, or new evidence; verify `instrumentEconomics / Compile / compile` succeeds and focused source inspection finds no prohibited implementation mechanism.
- [ ] 1.2 Add focused instrument-economics coverage that type-checks each alias in both directions with its role projection and observes unchanged instrument identity, grids, rates, refinements, and representative nested result values; verify `instrumentEconomics / test` passes.
- [ ] 1.3 Add completed-artifact Scala fixtures that use all four aliases in generic and representative concrete calls without explicit dimension arguments or local `D`/`B`/`Q`/`S` aliases, plus independently valid negative preludes rejecting cross-role and abstract cross-instrument values; verify `adversarialBoundary / Test / testOnly external.EconomicsCompilerBoundarySuite` passes with the intended diagnostics and retains the existing no-downstream-classpath and no-downstream-`Instrument` surface assertions.
- [ ] 1.4 Run `tools/check-in-process-reflection.sh`, `tools/test-check-in-process-reflection.sh`, `git diff --check`, and `sbt -batch scalafmtCheckAll scalafmtSbtCheck clean test benchmarks/Jmh/compile`; verify the complete JDK-25 repository matrix passes with no dependency, ownership, serialization, runtime-representation, or benchmark regression, then complete the automated Task Group review with no unresolved actionable finding.
