## 1. Direct Transition Sums

- [ ] 1.1 Confirm the effective RFC/source binding, exact integrated Slice 2 baseline, isolated worktree identity,
  pilot authority, closed native dependencies, and clean focused execution-lifecycle baseline.
- [ ] 1.2 Replace command and source-fact transition abstract-class families with sealed Scala sums whose generated
  cases retain state, classification or violations, and fail-closed serialization without hand-written equality.
- [ ] 1.3 Replace lifecycle accepted-kind tagging with distinct applied, idempotent, conflicting, and rejected sum
  cases that retain exact state, rejection, and work accounting.
- [ ] 1.4 Update production transition construction and consumption plus focused tests to exhaustively match every new
  command, source, and lifecycle transition case and preserve prior state/conflict/violation semantics.
- [ ] 1.5 Run focused command, source-fact, lifecycle-state, compiler-boundary, formatting, and serialization checks and
  inspect the group diff for transition-only scope.

## 2. Direct Knowledge and Effective-Fill Sums

- [ ] 2.1 Convert submission knowledge alternatives to generated Scala sum cases while retaining exact evidence,
  conflict precedence, uncertainty, authoritative absence, and structural equality.
- [ ] 2.2 Convert cancellation knowledge alternatives to generated Scala sum cases while retaining request,
  confirmation, conflict, and authoritative-confirmation semantics.
- [ ] 2.3 Convert active, busted, ambiguous, and conflicting effective-fill alternatives to generated Scala sum cases
  while retaining typed dimensions, modifier ordering, conflict evidence, and exact exposure.
- [ ] 2.4 Update derivations, owner-local consumers, focused/property tests, and completed-artifact Scala fixtures for
  exhaustive matches, equivalent fields/equality, and serialization rejection across all revised knowledge and fill
  cases.
- [ ] 2.5 Run focused submission, cancellation, effective-fill, ordering, compiler-boundary, and formatting checks and
  inspect the group diff for preserved lifecycle behavior.

## 3. Direct Derived Products and Guarded-Boundary Integration

- [ ] 3.1 Convert field-valid submission/cancellation evidence, command/source conflicts, effective-fill ledger,
  anomalies, lifecycle observation, and replay results to package-owned structural products with generated equality
  while retaining useful computed accessors.
- [ ] 3.2 Remove redundant forwarding constructors and manual equality/hash implementations for every converted value,
  leaving command state, source evidence state, execution state, checked inputs, identities, refinements, and non-empty
  wrappers guarded.
- [ ] 3.3 Strengthen completed-artifact Scala fixtures and focused tests to prove revised product fields, structural
  equality, exhaustive sum consumption, and Java object-serialization rejection.
- [ ] 3.4 Retain and run positive and negative coverage for commands, source facts, lifecycle construction, non-empty
  wrappers, identity/refinement values, dimension/grid association, and external reconstruction failures.
- [ ] 3.5 Run the complete clean aggregate test matrix, benchmark compilation, zero-reflection guard and regression,
  formatting checks, strict OpenSpec/Corgi validation, and final scope inspection against AC-008 through AC-011.
