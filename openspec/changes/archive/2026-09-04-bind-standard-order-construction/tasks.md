## 1. Instrument-bound standard-order construction and boundary evidence

- [ ] 1.1 Add one final immutable `Order`-owned instrument scope and a singleton-preserving `forInstrument` entry point; implement market, limit, stop-market, and stop-limit as dependent thin delegates with the current arguments, defaults, exact `Order.Aux` refinements, and canonical validation, while leaving explicit generic `create` and `createFirst` unchanged; verify `orderModel / Compile / compile` succeeds and focused inspection finds no cast, mutable/cache, effect, thread-local, or downstream-owner mechanism.
- [ ] 1.2 Add focused order-model tests comparing every scoped constructor with its direct counterpart for valid defaults and representative explicit options, exact refined types, signed position and identity, complete deterministically ordered invalid violations, independent successful instances, interleaved invocation-order invariance, concurrent reuse, and same-shaped foreign generic-component validation; verify `orderModel / test` passes.
- [ ] 1.3 Add completed-artifact Scala fixtures that construct concise market and priced orders from one scope without explicit dimension arguments, role projections, casts, structural refinements, or local dimension aliases, and independently valid negative preludes rejecting incompatible lots, prices, triggers, pegged pricing, and visibility; verify `adversarialBoundary / Test / testOnly external.EconomicsCompilerBoundarySuite` passes with intended type diagnostics and retains the existing order-model dependency boundary.
- [ ] 1.4 Run `tools/check-in-process-reflection.sh`, `tools/test-check-in-process-reflection.sh`, `git diff --check`, and `sbt -batch scalafmtCheckAll scalafmtSbtCheck clean test benchmarks/Jmh/compile`; verify the complete JDK-25 repository matrix passes with no order algebra, validation, error, ownership, dependency, serialization, or runtime regression, then complete the automated Task Group review with no unresolved actionable finding.
