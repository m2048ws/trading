# Trading Order Model

Pure immutable order instructions owned by `trading.order`.

The artifact depends only on quantities and instrument economics. It contains no market-state observations, matched
liquidity, scenario evaluation, fee or risk policy, application services, runtime effects, or order lifecycle state.
