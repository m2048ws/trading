## 1. Direct Associated-Evidence Construction

- [ ] 1.1 Confirm the effective RFC/source binding, integrated Slice 3 baseline, isolated worktree identity, pilot
  authority, closed native dependencies, and clean focused order/scenario baseline.
- [ ] 1.2 Remove erased activation-evidence and pricing-resolution acceptance hooks from order activation, pricing, and
  execution types without changing instruction-owned semantic verification.
- [ ] 1.3 Make `ScenarioAssumptions.create`, `one`, and `many` construct directly from associated Scala evidence and
  non-empty slices; retain the typed empty result in `fromVector`.
- [ ] 1.4 Update scenario, fee, codec, property, and completed-artifact Scala consumers for direct assumptions results,
  remove the erased-reflection test, and retain negative associated-shape compiler coverage plus same-shape semantic
  mismatch coverage.
- [ ] 1.5 Run focused order-model, execution-scenario, fee-policy, boundary-codec, compiler-boundary, and formatting
  checks and inspect the group diff for unchanged downstream semantics.

## 2. Retire Ordinary-Java API Fixtures

- [ ] 2.1 Inventory the reference-data, order/scenario, execution-lifecycle, and boundary-codec Java assertions against
  retained Scala compiler, semantic, dependency, wire, null-boundary, and serialization evidence.
- [ ] 2.2 Remove the ordinary-Java domain fixture resources and the dedicated reference-data Java boundary suite.
- [ ] 2.3 Remove Java-only compiler records, fixture roots, tests, compiler helpers, and classloader paths from mixed
  order/scenario, execution-lifecycle, and boundary-codec suites while preserving shared Scala completed-artifact
  execution support.
- [ ] 2.4 Run the complete adversarial-boundary test suite from clean completed artifacts and confirm every retained
  Scala positive/negative, dependency, wire, semantic, null, and serialization boundary still executes.
- [ ] 2.5 Inspect the group diff and repository fixture inventory to confirm the targeted Java domain API contracts and
  dedicated harnesses are gone without changing production Java sources reserved for S-04.

## 3. Scala 3 Source Contract and Full Integration

- [ ] 3.1 Align active repository architecture, quantity-grid, runtime-quantity, reference-data identity/catalog,
  order/scenario, execution-lifecycle, and boundary-codec specifications on Scala 3 as the supported source API.
- [ ] 3.2 Preserve explicit requirements for JDK 25, Java-library integration, checked external representations, exact
  decimal conversion, canonical wire formats, and Java-object-serialization rejection.
- [ ] 3.3 Run targeted source/spec scans proving no erased evidence hooks or ordinary-Java domain fixture obligations
  remain and that retained JVM/external-boundary contracts are still present.
- [ ] 3.4 Run the complete clean aggregate test matrix, benchmark compilation, zero-reflection guard and regression,
  formatting checks, strict OpenSpec/Corgi validation, and final scope inspection against AC-016 through AC-019.
