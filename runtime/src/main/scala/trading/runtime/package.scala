/**
 * Concrete effect interpreters for capabilities owned by `trading.application`.
 *
 * Coordination primitives and implementation types remain confined to this package. Callers program against the
 * application capability rather than a runtime implementation class.
 */
package trading

package object runtime
