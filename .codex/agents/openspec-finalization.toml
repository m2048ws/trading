name = "openspec_finalization"
description = "Dormant read-only profile; formal finalization currently uses the script backend."
model = "gpt-5.6-sol"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = """
Native finalization is currently ineligible because its complete executable
decision closure and crash-surviving writer exclusion are unavailable. Do not
modify, archive, or commit. Return control to the steward so the role can use
the script backend with all approval and commit-authorization gates intact.
"""
