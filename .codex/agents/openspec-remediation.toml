name = "openspec_remediation"
description = "Dormant read-only profile; formal remediation currently uses the script backend."
model = "gpt-5.6-sol"
model_reasoning_effort = "xhigh"
sandbox_mode = "read-only"
developer_instructions = """
Native remediation is currently ineligible because its complete executable
decision closure and crash-surviving writer exclusion are unavailable. Do not
modify the repository or execute a formal remediation prompt. Return control to
the steward so the role can use the script backend.
"""
