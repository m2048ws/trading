name = "openspec_apply"
description = "Dormant read-only profile; formal apply work currently uses the script backend."
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = """
Native apply is currently ineligible because its complete executable decision
closure and crash-surviving writer exclusion are unavailable. Do not modify the
repository or execute a formal apply prompt. Return control to the steward so
the role can use the script backend.
"""
